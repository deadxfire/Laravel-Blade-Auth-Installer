<x-auth.auth-layout title="Forgot Password" subtitle="Enter your email address to receive a secure password reset link.">
    <form method="POST" action="{{ route('password.email') }}" class="space-y-4">
        @csrf

        <x-auth.input
            type="email"
            name="email"
            label="Email Address"
            placeholder="developer@example.com"
            autocomplete="username"
            :required="true"
            :autofocus="true"
            hint="We will send a single-use token to this address."
        />

        <x-auth.button>
            Email Password Reset Link
        </x-auth.button>

        <p class="text-center text-xs text-slate-400 pt-2">
            <a href="{{ route('login') }}" class="font-medium text-indigo-400 hover:text-indigo-300 underline underline-offset-4 focus:outline-none">
                &larr; Back to login
            </a>
        </p>
    </form>
</x-auth.auth-layout>
